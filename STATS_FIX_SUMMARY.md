# 统计功能Bug修复总结

## 问题描述
用户反馈统计功能显示的专注次数和专注时长不正确，实际只专注了2次，但显示为23次。

## 根本原因分析

### 1. 数据迁移逻辑重复执行
**位置**: `src/scripts/modules/statistics.js` - `getStatisticsHistory()` 函数

**问题**: 
- 每次打开统计弹窗时都会检查是否需要数据迁移
- 如果检测到旧格式数据（包含 `count` 字段），就会将其拆分成多条记录
- 迁移后的数据被保存，但下次打开时可能再次触发迁移，导致数据累积

**修复**: 
- 移除了会重复执行的数据恢复逻辑
- 优化数据迁移逻辑，添加 `needsSave` 标志，确保只在真正需要时保存一次

### 2. 部分完成的番茄钟也计入次数
**位置**: `src/scripts/modules/stats.js` - `recordPartialFocus()` 函数

**问题**:
- 当用户重置未完成的计时器时，`recordPartialFocus` 函数会增加 `todayCount`
- 这导致"今日完成"次数包括了未完成的番茄钟

**修复**:
- 修改 `recordPartialFocus` 函数，使其不增加 `todayCount`
- 只有完整完成的番茄钟才计入"今日完成"次数
- 但部分完成的时长仍然会计入总时长

### 3. 统计概览显示的是时间段总和而非今日数据
**位置**: `src/scripts/modules/statistics.js` - `updateOverview()` 函数

**问题**:
- 在"每日"视图下，统计概览显示的是最近7天的总和，而不是今天的数据
- 用户期望看到的是今天的统计，但实际显示的是一周的累计

**修复**:
- 修改 `updateOverview` 函数，在"每日"视图下只显示今天的数据
- 在"每周"和"每月"视图下显示相应时间段的总和
- 添加动态标签，清楚标识显示的是"今日"、"最近4周"还是"最近6个月"

### 4. 历史记录统计包含部分完成的记录
**位置**: `src/scripts/modules/statistics.js` - `getLast7Days()`, `getLast4Weeks()`, `getLast6Months()` 函数

**问题**:
- 统计图表和详细数据中，专注次数包括了部分完成的记录

**修复**:
- 修改所有时间段统计函数，只计算完整完成的番茄钟次数（`!item.partial`）
- 但时长仍然包括所有记录（包括部分完成的），因为用户确实花费了这些时间

## 修改的文件

1. **src/scripts/modules/statistics.js**
   - `getStatisticsHistory()`: 优化数据迁移逻辑
   - `updateOverview()`: 修改为在每日视图显示今日数据
   - `getLast7Days()`: 只计算完整完成的次数
   - `getLast4Weeks()`: 只计算完整完成的次数
   - `getLast6Months()`: 只计算完整完成的次数

2. **src/scripts/modules/stats.js**
   - `recordPartialFocus()`: 不再增加 `todayCount`

3. **debug_stats.html**
   - 更新调试工具，显示完整/部分完成状态
   - 增强数据一致性检查

## 数据结构说明

### 历史记录格式
```javascript
{
  date: "2026-04-05",           // 日期（YYYY-MM-DD）
  timestamp: "2026-04-05T10:30:00.000Z",  // 完整时间戳
  minutes: 25,                   // 专注时长（分钟）
  note: "学习JavaScript",        // 备注
  partial: false                 // 是否为部分完成（可选，默认false）
}
```

### 统计数据格式
```javascript
{
  date: "Sun Apr 05 2026",      // 日期字符串
  todayCount: 2,                 // 今日完成次数（只计算完整完成的）
  totalMinutes: 50               // 今日总时长（包括部分完成的）
}
```

## 使用建议

1. **查看当前数据状态**
   - 打开 `debug_stats.html` 查看详细的统计数据
   - 检查是否有数据不一致的警告

2. **如果数据已经错误**
   - 使用调试工具的"修复数据"功能清除所有历史记录
   - 重新开始统计

3. **正常使用**
   - 完成一个番茄钟：`todayCount + 1`，时长增加
   - 重置未完成的番茄钟：`todayCount` 不变，但时长会增加（如果超过1分钟）

## 预期行为

### 主界面统计
- **今日完成**: 只显示完整完成的番茄钟次数
- **累计专注**: 显示今日总时长（包括部分完成的）

### 统计弹窗
- **每日视图**: 显示今天的数据
- **每周视图**: 显示最近4周的总和
- **每月视图**: 显示最近6个月的总和
- **专注次数**: 只计算完整完成的番茄钟
- **专注时长**: 包括所有时间（包括部分完成的）

## 测试建议

1. 完成一个完整的番茄钟，检查计数是否正确
2. 开始一个番茄钟后立即重置，检查计数是否不变
3. 打开统计弹窗，检查"每日"视图是否只显示今天的数据
4. 切换到"每周"和"每月"视图，检查是否显示正确的时间段总和
