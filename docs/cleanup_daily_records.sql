-- 清理旧的每日专注记录
-- 因为数据结构改变：现在每个用户每天只有一条记录（存储今日总统计），不再按room_id区分

-- 删除所有旧的每日记录
DELETE FROM daily_focus_records;

-- 说明：
-- 1. 旧数据结构：每个用户在每个自习室每天有一条记录
-- 2. 新数据结构：每个用户每天只有一条记录，存储今日总统计
-- 3. 排名显示：获取自习室成员的今日统计数据
-- 4. 用户完成番茄钟后会自动上传新的统计数据
